import React from "react";
import { Card } from "../ui/card";
import { getAllPosts } from "@/utils/markdown";

const ProjectCard = () => {
  return <Card>sagfvsrv</Card>;
};

export default ProjectCard;
